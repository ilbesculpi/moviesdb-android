<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Common -->
    <style name="AppTheme.Common" parent="AppTheme" />
    <style name="AppTheme.Common.Title" parent="AppTheme.Common">
        <item name="android:textSize">@dimen/title_size</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginTop">@dimen/margin_md</item>
        <item name="android:paddingBottom">@dimen/padding_md</item>
    </style>

    <!-- Home -->
    <style name="AppTheme.Home" parent="AppTheme.Common" />
    <style name="AppTheme.Home.ListItem" parent="AppTheme.Home">
        <item name="android:textSize">@dimen/title_size</item>
        <item name="android:textColor">@color/colorPrimaryText</item>
    </style>

    <!-- Movies -->
    <style name="AppTheme.Movies" parent="AppTheme.Common" />
    <style name="AppTheme.Movies.Title" parent="AppTheme.Movies">
        <item name="android:textSize">@dimen/title_size</item>
        <item name="android:textColor">@color/colorPrimaryText</item>
    </style>
    <style name="AppTheme.Movies.Date" parent="AppTheme.Movies">
        <item name="android:textSize">@dimen/text_sm</item>
        <item name="android:textColor">@color/colorPrimaryText</item>
    </style>

</resources>
